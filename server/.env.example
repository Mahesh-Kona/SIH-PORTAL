# Example environment file

PORT=4000

# MongoDB Atlas Connection URI
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.a0ik5zj.mongodb.net/sihdb?retryWrites=true&w=majority&appName=Cluster0

# Session secret (for auth if needed later)
SESSION_SECRET=changeme
