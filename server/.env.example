# Environment variables for SIH Portal backend

# Node environment (development | production)
NODE_ENV=production

# Server port
PORT=10000

# MongoDB Atlas connection string (replace placeholders with real values)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.a0ik5zj.mongodb.net/sihdb?retryWrites=true&w=majority&appName=Cluster0

# Secret key for sessions or JWTs
SESSION_SECRET=changeme

# Default admin credentials
ADMIN_EMAIL=admin@rgukt.ac.in
ADMIN_PASSWORD=SIH2024@Admin
